# Argo development instructions

- This repository contains an executable MVP and its longer-term architecture. Keep README capabilities and roadmap status aligned with runtime-tested behavior.
- Keep code, documentation, comments, and commits in English.
- Follow `docs/architecture.md` for execution boundaries and `docs/roadmap.md` for implementation order.
- Do not interpret sample targets or a populated JSON file as authorization to run a test.
- Keep scope enforcement in deterministic code outside model prompts. Every action requires a typed, validated contract.
- Do not add a general shell tool to the model-facing interface. Use fixed executables and argument arrays in reviewed adapters.
- Do not expose the Docker socket, host home directory, cloud CLI credentials, SSH agent, or secret vault to workers.
- Use Hush for credentials, with output redaction; never store credential values in configuration, prompts, fixtures, logs, or reports.
- Use static imports. Add integration tests for backend functions and adapter behavior, including denial cases and failure paths.
- Runtime-test behavior changes before committing or pushing. Do not count type checking alone as runtime validation.
- Keep fixture tests offline and isolated. A live test requires an explicitly selected engagement.
- Keep dependency versions, scanner images, rule packs, and model artifacts pinned and attributable.
- Do not add tool or assistant co-authorship to commits or pull requests.

## Implemented runtime

- Python package: `src/argo`, managed with `uv.lock`. `argo` launches the Textual TUI. Plain text starts the isolated tool agent; `/chat` is advisory. CLI adds `agent`, `mcp-tools`, source `--import` and verified workspace `--continue` alongside the legacy engagement commands.
- `tui.py` and `data/tui.tcss` implement conversation, scope/authorization forms, findings, report/evidence viewers, and saved-run browsing. Follow `DESIGN.md`; test wide and 80-column terminals with Textual Pilot. Use worker threads and main-thread callbacks; never block the UI event loop with inference or scanners.
- `services.py` supplies shared readiness, run-path validation, and the owned synthetic demo. `controller.run` accepts progress and cancellation callbacks for the TUI. Escape requests cancellation; Ctrl+Q waits for active work to stop before closing.
- `contracts.py` defines strict Pydantic engagement, authorization, finding, package, and proposal models. `scope.py` canonicalizes targets and binds authorization to the full execution configuration with expiry. The structural JSON schema is separate from runtime checks.
- Legacy audit proposals remain `explain`/`review_evidence`. `agent.py` adds a separate executable loop: workspace read/list, coder edits, Python/Node tests, pytest, Bandit, project-matched CVE intelligence, specialist review and policy-limited MCP. The model chooses `action` before `parameters`; validate both the envelope and the selected tool schema. Tool output never grants scope or configuration changes. Python edits require successful pytest against unchanged files. Other text edits explicitly record missing runtime verification.
- Preserve bounded assistant action/tool-result conversation turns and the original operator task. The separate coder must receive the task, current instruction, selected files and recent test feedback. Replacing this with a repeatedly serialized task lost context in live model tests. Python output is syntax-checked; new unavailable dependencies receive bounded repair attempts.
- Coding profiles in providers.py select Ollama, OpenAI Chat Completions-compatible or Anthropic Messages-compatible endpoints and arbitrary model IDs. The profile supplies coordination and coding. ModelSettings persists the selection atomically in ~/.argo/models.json. Legacy specialist audit/chat remains local. Never silently fall back to another provider.
- workspace.py uses the operator-selected read/write project mount from the CLI/TUI; only that directory is exposed. Disposable mode has no bind mounts. Use the operator UID/GID for mounted writes, no network, read-only root and resource limits. Inspect the exact mount and preserve descriptor-relative no-follow file access. Never accept model-selected Docker flags, paths or images.
- `worker/Dockerfile` pins the base digest and `worker/requirements.txt` hashes dependencies. Rebuild with `scripts/install_worker.py` after worker changes; runtime uses the recorded local image digest. `data/agent/remote.py` runs in a separate bridge-network container with no code workspace. It implements bounded HTTPS MCP with public DNS/IP pinning, JSON/SSE, sessions and no server callback execution.
- mcp.py intersects operator policy with discovered schemas. Public remote MCP works; authenticated/stdio MCP remains unimplemented. Reject references, regex constraints and unknown tools. Coding-provider credentials use Hush through a fixed provider_worker child; keys never enter settings, prompts or code workers.
- Reports retain kind: isolated_agent and include project and coding_profile. The launch directory is mounted read/write by default; /workspace selects it, /isolated and /import select disposable mode. Changes persist immediately. /resume never mounts a report-supplied path; --continue restores only to disposable mode. /reset retains the selected project.
- Agent tool records retain exit codes and test-file hashes. `agent_demo.py` checks the generated implementation in a fresh container and requires failing-then-passing unchanged regressions, production changes and independent controls. Validation runs before durable completion; failures must remain failed in the report, state and evidence. Redaction that changes exported code requires retesting.
- `sandbox.py` runs digest-pinned Semgrep/Gitleaks with fixed argument arrays, no network, reduced privileges, deadlines, and output limits. The CLI enables them with `--scanners`; TUI audits enable them by default. Gitleaks sees originals through stdin and retains only redacted results.
- `network.py` is the native scoped HTTP broker. `webchecks.py` provides bounded CORS and Git HEAD checks; the seeded SQLite validation protocol belongs to the local lab. There is no production HTTP API server, general network scanner, Nuclei/ZAP worker, or authenticated role-matrix workflow.
- `intelligence.py` uses typed OSV/NVD/EPSS/CISA KEV clients with explicit disclosure policy and offline cache behavior. The external CVE MCP sidecar remains disabled. Credential references exist in the design, but authenticated execution is not implemented.
- `evidence.py` stores private run directories under `~/.argo/runs`, SQLite state/events, redacted evidence JSON, integrity manifests, and Markdown/JSON reports. Evidence readers validate identifiers, regular-file bounds, and hashes. Local hashes are not signed attestations.
- `/resume` restores verified agent files for the next task or reopens a legacy report. It does not restart interrupted processes or replay side effects. Advisory conversation history is memory-only. Retest reports "not detected" separately from "fixed".

## Verification

Run `uv run ruff check src tests scripts`, `uv run python scripts/validate_design.py`, and `uv run pytest -q`. The full suite needs Docker, installed worker/scanner images and loopback fixture servers. It executes code, failing/passing regressions, isolation probes, symlink denials, descendant cancellation, MCP protocol contracts and TUI workspace continuation at both terminal widths. Model contract tests are fixtures; the internal `agent_demo.py` fixture separately exercises real local models and public MCP with independent implementation checks. `argo demo --scanners` remains the legacy audit smoke test. See `docs/validation.md` and `docs/isolated-agent.md` for boundaries and unmeasured evaluation gates.

`uv build` must include `data/tui.tcss`, scanner locks/rules, and `data/agent/*.py`. `scripts/capture_tui.py` exports Textual screenshots. CI builds the isolated worker before integration tests. Never equate model-generated tests or a scanner alert with independent security validation.

The 0.2 local validation recorded 85 passing tests, actual three-model SQL repair with public MCP, independent code-creation checks and real-model TUI continuation. See the run IDs and limits in `docs/validation.md`; these smoke checks do not establish broad model reliability. Scroll conversation updates after layout and verify restored agent guidance at both terminal widths.

## Version 0.3 interaction and validation

- F2 or /model opens ModelDialog with profiles, protocol, URL, model ID, discovery, a synthetic test, Hush name, JSON mode and token parameter. Persist only on Save; keep model selection independent of the project. Test both terminal widths and HTTP-to-Docker edits.
- Provider tests cover real HTTP request/stream contracts for both compatible protocols, auth delivery in the fixed child, errors, persistence and direct writes. Use temporary projects; legacy TUI tests explicitly pass project=None so they cannot modify the development checkout.
- Mounted projects expose all their files to generated Python. Do not claim they have no host-file access or no project secrets. Adapter snapshots omit hidden/dependency/build paths and remain bounded. In-flight conflict checks do not provide transactional isolation. Cancellation does not undo writes.
- Test actual models separately from protocol fixtures. API compatibility tests do not establish model quality or a connection to a paid vendor.
- CodingProfile stores name, protocol, base_url, model, credential reference, output_mode, token_parameter, optional max_tokens and optional context_window. ModelSettings stores active and up to 30 uniquely named profiles. Preserve atomic private writes and reject credentials embedded in URLs.
- Compatible provider requests use /chat/completions or /messages and discover through /models under the configured URL prefix; Ollama uses /api/chat and /api/tags. Support SSE and JSON responses, bound their size/time, validate generated objects against the requested schema and report HTTP failures without response-body disclosure.
- Mounted worker writes accept expected original contents to detect changes during inference. Exports allow 1000 files in project mode and 100 in disposable mode, with a 2 MiB aggregate budget. Keep these limits aligned between worker and controller.
- Version 0.3 validation passed 100 local tests and a real Qwen TUI run through an OpenAI-compatible endpoint (0fc9d660326c48a68e9645eb27b975f7). The run repaired the mounted calculator source, retained the existing regression and passed an independent fresh-container check. The wheel includes provider_worker.py and model_dialog.py; see docs/validation.md for evidence and limits.
- OpenRouter was runtime-tested at https://openrouter.ai/api/v1 with meta/muse-spark-1.3-contributor, JSON schema mode and Hush-backed authentication. Run b9381aee41d24e9f82e9df85141a43a9 passed actual TUI connection, mounted-file repair, unchanged regression and independent container checks. Keep credentials and operator profile settings out of the repository.
- ModelDialog's synthetic connection test initially permits 2,048 output tokens: 256 truncated Muse's response during reasoning. Preserve truncation rejection and the HTTP/TUI regression covering this case. The initial integration baseline had 101 passing local tests; current counts are recorded below. Actual paid-provider validation remains separate from fixtures.

## Dynamic context and model activity

- `context_budget.py` models API-discovered context/output limits. `providers.model_limits` caches per endpoint/model/credential/override for five minutes and refreshes at task start. OpenRouter metadata comes from GET /models; Ollama capabilities use POST /api/show. Local Ollama num_ctx remains 16,384 unless explicitly overridden. Unknown metadata uses an explicitly labelled fallback; never silently substitute a provider.
- Context and output caps are optional in CodingProfile and editable in F2. Null means automatic. Preserve an existing cap when editing a profile. Estimate input from UTF-8 text with framing and 10% headroom. Automatic output starts proportionally to prompt/model capacity and grows on truncation, at most four inference requests up to the advertised/remaining ceiling. Never repeat a tool as part of a provider retry.
- `conversation.py` retains full observed results and auto-compacts at 90% of the input budget. Keep the original task, recent results, deterministic tool ledger and latest test feedback. Summaries are untrusted reference text. Persist context_compaction evidence and atomically replace context.json before discarding old in-memory turns. Failed/cancelled compaction or checkpoint failure must preserve history; test repeated compactions in one run.
- Reports add context_compactions evidence IDs. New tasks still start new conversations; /resume restores result views or files, not prior conversation memory. Do not claim cross-run session continuity or exact tokenizer accounting.
- ModelLimits contains context_window, optional max_output_tokens and source. The provider accepts context_length/context_window/max_input_tokens and top_provider capacity fields. Context checkpoints retain operator_task, summary, evidence_id and compactions; immutable evidence also records before/after estimates and summarized turn count. Keep JSON writes private and atomic across repeated checkpoints.
- ProviderResponseError preserves truncation, filtering, incomplete response, malformed JSON and schema/format categories through the credential child. ProviderHTTPError retains status, safe Retry-After and upstream shared-pool classification; never surface raw provider error bodies. HTTP 429 does not cause automatic provider switching or repeated quota retries.
- Provider HTTP responses and credential-child request input allow 16 MiB; the shared child-process output bound remains 8 MiB. These transport limits and the 300-second response deadline are separate from context-window capacity. Classify malformed metadata/response shapes without including their body in errors.
- The TUI has a five-line ASCII welcome, an always-visible coder and three-reviewer roster, and F3 /models for role/activity/live response views. Source reviews stream API-exposed local reasoning in a separate muted message; show loading with elapsed time before output, label provisional answers and render text literally. Keep reasoning out of validated evidence and coordinator inputs. Saved specialist responses come from verified tool evidence. The public agent-demo command is removed; retain its internal regression fixture.
- ModelDialog keeps only profile, connection, endpoint, model and credential name in the main form. Advanced is collapsed by default and retains profile naming, response format, output token field and token overrides. Show OpenAI compatibility controls only for OpenAI-compatible profiles and discovery choices only after listing. Clear discovery when changing connection/profile/endpoint. Preserve saved advanced values when hidden; verify expansion, discovery and saving at both terminal widths. Keep explanatory prose out of the form and use concise action feedback.
- The simplified settings layout passed 36 provider/TUI/model-activity tests, including hidden-value persistence and discovery reset. The refreshed docs/assets/model-settings.svg captures the main form; review wide and 80-column layouts when changing field spacing or collapsed controls.
- Local review source selection uses token-budgeted batches instead of the old 24,000-character rejection. Preserve all source text across batches, expose batch count and keep findings provisional pending tests. Cross-batch analysis can miss relationships.
- September 7 runtime validation: 121 full-suite tests passed; actual Foundation-Sec and VulnLLM emitted 14 and 15 live updates. OpenRouter reports Muse context 1,048,576 and max completion 943,718 tokens. Adaptive 32,768-token initial output succeeded; requesting the full advertised output ceiling encountered upstream shared-pool HTTP 429. Actual Muse compaction reduced approximately 26,897 estimated tokens to 337. Run 6320518f378c4c959806896e0b10cf3b repaired a temporary mounted calculator with unchanged failing-then-passing pytest and an independent fresh-container pass. These checks establish integration behavior, not broad security accuracy.

- `agent_findings.py` defines the typed findings.record contract and maps local review/structured script observations to Finding records. Validate selected-workspace paths and existing evidence IDs before registration. Model/script records always remain suspected, including legacy verdicts such as sfruttabile; info means unassessed severity. Persist findings in report.json/report.md and finding_count, and publish them to the live TUI.
- Empty older agent reports recover structured findings on read from verified security.review/python.run records (including titolo/file/evidenza aliases), never from final prose. Reject malformed paths and raw evidence citations in tool output; keep original evidence and manifests unchanged. Test tampering, unknown evidence, missing source paths and old-run display/counts at both widths.
- Local source reviews query POST /api/show for thinking support, then stream message.thinking separately from message.content. Capability support does not guarantee that a custom model will emit reasoning on every prompt. Foundation-Sec supports native thinking; the installed VulnLLM does not advertise it. Legacy audit/advisory paths retain their prior behavior.
- Failed single-specialist reviews retain model/status/error in tool evidence, like concurrent reviews. Resume restores the error in Models. Older runs without a review record recover only the matching model-prefixed coverage gap for display; existing reports and manifests remain unchanged.
- Foundation-Sec and VulnLLM source batches reserve an 8,192-token output allowance within the 16,384-token context. Generation starts at 4,096 and increases to 8,192 after truncation, before the bounded source-subdivision recovery described below. Inspect done_reason before accepting JSON. Keep their 2 MiB stream bounds, specific sanitized error messages, cancellation between chunks/attempts and specialist coverage gaps. Foundation-Sec uses temperature 0.3 with a 1,200-second request deadline; VulnLLM retains temperature 0 and 300 seconds. Do not restore greedy Foundation-Sec generation: the local TypeScript control exhausted both 4,096 and 8,192 tokens without an answer at temperature 0, while the same prompt completed with 2,448 generated tokens at 0.3. This one comparison is integration evidence, not a reliability benchmark. Both retain 120-second idle-read limits. Tasks selecting Foundation-Sec allow four hours overall so a longer review or truncation retry is not cut off by the former 30-minute task limit. Qwen has the separate limits documented below; neither configuration caps the cloud coding model context.

- Final reasoning/findings validation passed 138 full-suite tests, followed by 19 targeted tests and real saved-run UI checks after column sizing. A real Foundation-Sec TUI call displayed native reasoning before the answer. Preserve responsive Findings columns, the full source location in the detail pane and the distinction between confirmed local test behavior and model hypotheses. Live specialist checks still show false positives and false negatives.
- `findings.record` accepts 1–20 entries with title, path, severity, explanation, remediation and 1–10 existing SHA-256 evidence IDs; line is optional or null. Persist the registration itself as agent_tool evidence. Progress events carry separate reasoning, text, status and findings fields. TUI recovery is a read-time projection, so legacy report files and SQLite counts remain untouched even when the UI shows recovered observations.


## Third local reviewer and parallel execution

- `SPECIALISTS` maps foundation, vulnllm and qwen to pinned Ollama aliases. `argo-qwen:27b` uses mradermacher's Q4_K_M of trohrbaugh/Qwen3.8-27B-heretic-ara. Keep the revision, exact size and SHA-256 in config/models.lock.json. The third model is an experimental general reasoning reviewer, not a security fine-tune. Legacy audit/chat still use the original two adapters.
- Qwen source review starts at 32,768 context (growing within Ollama metadata limits for complete evidence), 8,192→16,384 output, temperature 0.6, a 3,600-second request deadline, a 600-second idle timeout and an 8 MiB transport bound. Local budgets never change Muse or other coding-provider metadata/overrides. Qwen/all-review tasks allow four hours overall; individual tools remain bounded.
- `security.review_all` accepts paths and optional 1–3 known CVE candidate_ids and dispatches three read-only workers over one source snapshot. A bounded 64-event queue returns progress/results to the controller. Never call SQLite, evidence mutation or TUI widgets directly from reviewer threads. Preserve a separate security.review tool/evidence record per reviewer, including status and model. Aggregate reviews include evidence_id and status; failed reviews create coverage gaps and do not satisfy required_reviews. Save successes before all workers finish so cancellation preserves them.
- `wait_local` checks cancellation every 0.2 seconds during pending HTTP operations, including silent loading. Cancel and await pending tasks before closing the client. The TUI must retain independent timers/states and reuse messages under interleaved events; cancelling one run stops all unfinished roles without overwriting completed responses. Follow the output tail only while its view is at the bottom; preserve manual scroll positions, and apply scrolling after layout with immediate=True.
- `scripts/evaluate_reviewers.py` executes owned SQL injection and object ownership positive/negative controls before model review, saves source hashes, per-model answers and timings in private files, and supports serial/parallel comparison. Its four tiny controls measure these cases only. Use protocol fixtures and live inference separately; a submitted concurrent request is not evidence of simultaneous GPU execution.
- `scripts/install_models.py` reuses an existing source blob only after size and SHA-256 verification, rejects file symlinks, and reserves space for GGUF conversion/validation copies. A fresh download requires four model sizes plus 5 GiB; an import from an existing Ollama source blob needs space for two additional copies plus 5 GiB. Do not remove installed model layers to free space or skip Ollama compatibility validation.
- Final integration validation passed 156 full-suite tests and built the source distribution/wheel. Actual Muse-coordinated run f92408b3acee4d15a322eb3ae839c197 completed all three reviewers, saved six suspected observations covering two owned control defects, retained unchanged files and verified its evidence manifest. On the 64 GiB Mac, the three output intervals overlapped, Ollama reported 32.44 GB combined GPU allocation, and swap did not increase. This establishes fit and concurrent operation under the tested context sizes, not broad accuracy or a speedup. Keep detailed measurements and customer-code evaluations private.

## Coordinator CVE intelligence and Node validation

- `project_inventory.py` parses npm lockfiles, Yarn v1, uv/Poetry and exact requirements, plus known Docker runtime images. Preserve exact versions, manifest hashes, declared dependency groups, import hints and explicit gaps. Do not infer deployed versions from locks, turn import hints into reachability claims, disclose unknown/private package scopes or query unpinned ranges. The dedicated worker manifests action has 100-file/2 MiB limits and the same no-follow access checks; normal source export remains 96 KiB per file.
- `advisories.py` integrates native OSV POST /v1/querybatch and GET /v1/vulns/{id}, NVD GET /rest/json/cves/2.0 (cveId/cpeName), FIRST GET /data/v1/epss and CISA KEV's fixed JSON feed. Existing intelligence.py owns fixed provider origins and transport limits. The external CVE MCP sidecar remains disabled and is not required by this flow.
- Preserve OSV per-query pagination, hydrated record/package identity checks, withdrawn-record rejection and provider provenance. Limits: 1,500 inventory packages, 100 queries per batch, five pages, 100 hydrated advisories, 200 candidate matches, 20 NVD CPE results, 100 enriched CVEs and 30 candidates per tool page. Truncation, unsupported formats, stale/missing/malformed data and manifest inconsistencies must create coverage gaps, never clean results.
- The private advisory cache uses request hashes and a 24-hour freshness window; failed connected refreshes may return explicitly stale entries. ~/.argo/intelligence-settings.json stores only mode offline/connected, controlled by TUI /cve and CLI --intelligence. Fresh installations and direct run_agent library calls default offline. TUI/CLI read the saved setting from the state directory's parent. Provider requests disclose only derived public tuples/CVE IDs/CPEs, never source text or arbitrary model URLs.
- Agent tools add security.inventory, security.cves, security.advisory and security.validation. Connected security tasks automatically inspect/query before coordination and before reviews as needed; manifest fingerprints invalidate catalog reuse. Reports retain intelligence mode/inventory/catalog, candidate source/freshness, and unresolved coverage. Discovery cannot change mounts, coding providers or scope.
- security.review and security.review_all accept up to three known candidate_ids and deduct bounded advisory context from each local model's source budget. cve_assessments must cover every supplied candidate exactly once, with assessment, reason, prerequisites and test_plan. Persist each completed response independently. Finding adds advisory_ids and strict ApplicabilityReview records (model, assessment, reason, prerequisites, test_plan, evidence_id). Catalog rereads and later reviews must preserve prior runtime evidence; model agreement does not confirm exploitability.
- `worker/Dockerfile` copies the Node 22 binary from digest-pinned node:22-bookworm-slim into the pinned Python worker. Rebuild/install the worker after any worker changes. node.tests invokes only node --test --test-reporter=tap on 1–40 tests/argo-security/*.test.cjs files under the same offline, non-root, read-only-root limits. No npm, package installation, generic shell, Docker socket or extra host mount is exposed to models. Dependencies must already be available in the selected project.
- security.validation binds a known CVE candidate to 1–5 actual same-run test records with test hashes and a model interpretation. Reproduced/not_reproduced requires a passing control run, while blocked can reference failed execution. Keep status suspected; generated controls and interpretation are not independent confirmation. Preserve tested file snapshots/hashes and explicitly report other untested language changes.

- Post-push verification: 168 full-suite tests, Ruff, design validation and distribution builds passed. Dedicated offline Node controls executed against the real jsonwebtoken 8.5.1 dependency: missing-key unsigned input was accepted, the protected verifier rejected it, and a valid signed identity was accepted. These three owned controls establish that fixture behavior only; keep HTTP/model contract fixtures separate from actual model inference when reporting validation.

## Isolated test database

- `test_database.py` defines the operator-selected `off|mongodb` fixture and pinned multi-platform image. CLI `--test-database` and session-local TUI `/test-db` pass the selection to `run_agent` and `Workspace`; model actions and report resumes cannot change it. Installation uses `scripts/install_test_database.py`; missing images fail explicitly.
- Start MongoDB in the validated worker's network namespace by container ID. Keep the worker on `--network none` and MongoDB on loopback with no published ports, project mounts or persistent volumes. Use read-only root, fixed UID, dropped capabilities, bounded tmpfs and resources. Inspect sidecar isolation and require readiness before model execution. Remove the database before the worker on normal exit, failure and cancellation; each task starts empty.
- Pass only the fixed synthetic `ARGO_TEST_MONGODB_URI` to worker executions. Persist fixture provenance separately from production configuration. Tests must use real database operations and verify external networking stays unavailable. Native host dependencies do not become Linux-compatible by selecting a fixture.
- Tool schema errors identify the failing field and constraint without echoing source. In particular, excessive `code.edit.instruction` length must ask the coordinator for concise instructions, rather than returning a truncated copy of its entire attempted implementation.
- Validation passed 176 full-suite tests, followed by 13 reviewer/report tests with warnings treated as errors after correcting the async test harness. Checks also passed for Ruff, design checks and source/wheel builds. The pinned MongoDB index was checked for Linux arm64 and amd64 manifests; runtime validation ran on the local Apple Silicon host. Real database controls proved insert/find, empty state in consecutive tasks, network denial and cleanup; these do not prove compatibility with arbitrary application test runners.

## September release verification

- `finding_review.py` enforces automatic local Qwen review of conclusive finding verdicts and verified fixes. Preserve deterministic test gates before inference, separate before/after evidence, strict decisions, current source/test binding and accepted-context reuse. A generic `security.review` response cannot satisfy this gate. Rejections and missing context remain unresolved; explicit blockers remain incomplete. Persist both phases through tests, deferral and evidence recovery, and show them in Findings/Models without rendering the source prompt as model output. General coding without findings does not require Qwen.
- `FindingVerification.reviews` stores strict `FindingReview` entries with model, phase, proposed verdict, completion status, decision, summary, test assessment, remaining concerns, evidence/test IDs and context fingerprint. Only complete `agree` decisions satisfy a gate. Preserve original `FindingReproduction` evidence through subsequent tests and deferrals. Changes to a reviewed claim invalidate its verdict; deferral must retain declared source paths. The controller returns validated review conclusions with verdict results while keeping source snapshots and native reasoning out of coordinator observations.
- Mandatory review contexts include complete declared source, tests/support, manifests and runtime records; fix review adds the accepted original snapshot. Reject oversized contexts explicitly instead of subdividing or dropping locked tests. Generic source reviews retain their separate batching/subdivision behavior. `review_response` shares bounded generation/transport retries between both adapters, without replaying execution tools.
- Release `7a58af6` passed 321 local tests, Ruff, design checks and source/wheel builds. Actual GLM/Qwen run `6934c7bc8c554220b319276cc08a1254` completed both mandatory phases and seven unchanged tests on an owned authorization fixture. A separate operator-written 20-case matrix confirmed the exact before/after sources in fresh workers. Keep this scoped validation distinct from full-project coverage and model accuracy; earlier Omnipass runs do not acquire retrospective Qwen approval. Final assessment-forwarding and UI-detail changes were validated through HTTP/runtime/TUI fixtures after that live process started.

- `finding_validation.py` enforces a per-finding runtime queue for reviewer, script and database observations. Every entry needs `findings.test` with separate secure-behavior regression/positive/negative controls and `findings.verdict`, or an evidence-backed `findings.defer` blocker. Pending/tested/stale entries block finish; inconclusive entries retain incomplete run status. Legacy `security.validation` attachments do not satisfy this gate. Preserve queue IDs in controller context across compaction and paginate full details through findings.list.
- Supply an explicit next-finding task with suggested test filenames. Before pending findings are assessed, code.edit is limited to tests/argo-security/. Reject imports-only/placeholder Python finding tests during bounded coder repair, before writing. Preserve fixture/conftest hashes when invalidating old results; only new unrelated dedicated test files are exempt. These structure/hash checks do not establish semantic relevance or independent proof.
- Keep runtime outcomes separate from the original suspected Finding status and from independent validation. Reproduced requires actual assertion failure plus passing controls; refuted requires passing regression and controls for the tested scenario. Empty/skipped/xfail, collection/import/setup/runtime errors cannot satisfy either. Tests must exercise real project behavior; passing trivial, mocked or static tests is not semantic proof. The fixed worker finding_pytest.py collector distinguishes assertion failures from execution errors. Node empty-file success must remain inconclusive. Rebuild the worker after adapter changes.
- Bind each finding's latest same-run test evidence to captured source/manifest and test hashes, preserve historical evidence, and invalidate verdicts after relevant edits. New unrelated tests do not invalidate completed work. Hashes do not attest excluded/hidden/oversized files, installed dependency contents or deployment. Dedicated failing finding tests can complete a source-unchanged audit; implementation edits still require existing runtime gates. Mounted-project default steps are min(256, 24 + visible file count) + 8 per finding, capped at 1,024; disposable fixtures retain the 24-step base; explicit 1–40 step limits are strict. Never hide unfinished verification behind a complete status.

- Local review recovery subdivides only length-failed source batches after both output budgets are exhausted, at most two levels. Preserve every source character, original-path line ranges, redacted segment hashes, completed/unreviewed paths and prior successful responses. Failed reviews retain partial evidence/findings through both single and concurrent adapters and must not satisfy required_reviews. Retry local transport errors and HTTP 429/500/502/503/504 once per output allowance with cancellation during backoff; do not retry hard budgets, authentication failures, edits or tests. Review fragments and synthetic test fixtures must not be described as complete deployed source.
- A source-unchanged audit can complete when its evidence copy is redacted; retain an explicit warning that the copy is not a tested build. If workspace edits occurred and export redaction changes text, preserve the incomplete status and retesting requirement.
- `providers.exchange` adds OpenRouter app attribution inside the credential worker as well as direct requests: `HTTP-Referer` uses the stable Argo repository URL, `X-OpenRouter-Title` is Argo, and the category is cli-agent. Only the exact HTTPS openrouter.ai origin on port 443/default receives these headers. Never derive the app URL from the selected workspace, model response or arbitrary provider settings; preserve other providers' headers.
- OpenRouter generation requests receive a stable random-run/role `x-session-id` and OpenAI-compatible `stream_options.include_usage`. Keep them out of other endpoints and discovery. Preserve session identity across retries, separate coordinator/coder/compactor roles, and never use workspace paths or prompt content as identifiers. `provider_usage` evidence and weighted report summaries include available counters from failed attempts; missing counters remain unknown. Keep usage outside model context, and preserve normal context limits, compaction and provider fallback behavior.

- The first reproduced verdict freezes original failing evidence, all three test roles/paths/hashes, existing test support and declared source paths. `fixed` requires a declared source change followed by the same unchanged tests all passing. Protect original tests/helpers from coder edits and reject added test configuration; never relabel a reproduced finding as refuted after a repair. Keep the underlying Finding status suspected. Preserve original evidence through staleness, deferral and recovery. Controller-computed invalidation IDs in tool evidence distinguish actual mutations from nonmutating test runs; legacy recovery remains conservative.
- `project.tests(runner="vitest")` executes only the installed Vitest entrypoint with fixed serial flags and a bounded JSON report. Require nonzero test counts, no failures/skips, exit zero and unchanged visible source/manifests. Its deadline is 300 seconds; other execution remains 50 seconds. Preserve partial output and an inconclusive exit 124 when the suite reaches an execution limit. No package installation or generic shell is added. An attempted project suite must pass against current edited files before finish. Test the actual runner separately from protocol fixtures and preserve narrower node.tests coverage.
- `test.database.reset` requires the operator-selected MongoDB fixture and accepts no configuration. Restart only the task's generated database container, preserving the code worker, project and locked tests. Record the new container ID/reset count, require tests to seed their own records, and preserve cleanup on restart failure or cancellation. Never accept a model-selected database address or name.
- Node finding-test generation rejects flattened/comment-only files and explicit process termination before writing; require multiline test/it registrations and preserve source newlines in JSON. CJS/MJS syntax validation uses fixed node --check in temporary worker files and never executes source. Feed validation failures into the existing bounded coder repair loop. Nested TAP assertion diagnostics may include parent subtestsFailed aggregates; only actual assertion leaves can reproduce a finding, and mixed runtime/setup failures remain inconclusive.
- Preserve bounded partial output when a finding test reaches its execution deadline/output budget. Kill the process group, return exit 124 and an inconclusive outcome, and retain the workspace for an explicit same-test retry. Never infer success from partial TAP output or automatically replay tests through provider retry logic. Outer adapter failures still close the worker.
- `code.edit.context_paths` optionally selects existing visible reference files without reducing model capacity; omitted selection retains workspace filling within the API budget. Reject unknown/outside paths. OpenRouter JSON modes require parameter-support routing without provider pinning. Preserve numeric HTTP statuses inside generation-stream error events and retry only known transient statuses or Anthropic api_error/overloaded_error; never leak error bodies or retry auth/billing/rate-limit failures.

- Commit `0162694` publishes the isolated MongoDB fixture, restored failed-review evidence and Foundation-Sec generation recovery. The complete local suite passed 184 tests and the GitHub Test workflow succeeded on that commit.
- Foundation-Sec completed the previously failing four-file prompt in 177.31 seconds at temperature 0.3 with 2,448 generated tokens. A separate check through the installed Argo environment completed the owned controls in 21.96 seconds. Preserve the distinction between completed inference and verified findings: the model also produced unsupported observations, which were not applied.

- Qwen generation sizes local `num_ctx` from the complete prompt/schema, maximum output reserve and 10% headroom, rounded up by 32,768 tokens and bounded by the architecture-specific capacity in Ollama `/api/show`. The installed artifact reports 262,144. Missing/invalid metadata falls back to 32,768; reject oversized contexts before chat. Never omit lockfiles or before/after evidence to fit. Small-source review batching remains unchanged, and larger-context concurrency/memory fit requires separate measurement. HTTP integration tests exercise full large manifests, complete before/after payloads, capacity failures and invalid metadata; a Docker controller test verifies inventory reads beyond 24 steps while explicit limits remain strict.

- Allocate the four-hour task deadline before inventory for connected security audits or declared required Foundation-Sec/Qwen reviews. Waiting until the first review incorrectly stopped a full project inventory after its 30-minute baseline plus a transient-provider retry allowance. Keep ordinary tasks at 30 minutes, retain their existing expansion on first specialist invocation, and preserve per-request/cancellation/explicit-step bounds. Runtime HTTP/Docker fixtures simulate long initial reading, ordinary-task expiry and the four-hour ceiling without sleeping for those durations.

- Coding-provider HTTP 522 and 524 gateway timeouts use the existing five bounded generation retries, including numeric errors inside a successful HTTP stream. Keep identical request context, cancellation, retry evidence and terminal exhaustion; never replay completed execution tools or retry authentication, billing or rate-limit failures. These transport retries do not alter API-derived context/output limits. HTTP and Docker integration fixtures cover recovery after completed tests and exhaustion.

- The Node TAP diagnostic loader preserves the first mapping value when Node flattens an assertion's actual Error into duplicate keys. Keep outer assertion identity distinct from its nested cause: failed doesNotThrow/doesNotReject assertions can reproduce a defect, while uncaught runtime/setup errors, including those wrapping an assertion, remain inconclusive. Preserve mixed-failure and parent-subtest checks and rebuild the worker after parser changes.
- Coordinator status exposes exact latest per-finding test IDs and prioritizes tested findings awaiting interpretation. Deterministic verdict guidance preserves existing freshness, outcome and original-test gates; eligibility never substitutes for bound Qwen approval. Finding-test observations retain actual diagnostics, counts and test hashes while complete source/support hash maps remain in durable evidence and mandatory review context. Preserve these pointers and outcomes across compaction without changing model capacity.
- Retain rejected actions, specific recovery guidance and error streaks as agent_action_error evidence, indexed by report.action_errors. Five consecutive rejected actions, including premature finish attempts, stop with incomplete status; a successful action resets the streak. Never manufacture a verdict, change test classifications or replay side effects to escape a loop.
